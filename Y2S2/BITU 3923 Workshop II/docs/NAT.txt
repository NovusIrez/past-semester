NAT overload

R1(config)#ip access-list standard <NAT>
#permit 10.10.0.0 0.0.255.255
#permit 10.20.0.0 0.0.63.255
#permit 10.30.0.0 0.0.0.255
#permit 10.40.0.0 0.0.1.255

R1(config)#int e0/0
R1(config-subif)##ip nat outside
R1(config-subif)##nat64 enable
R1(config)#int e1/2.10
R1(config-subif)##ip nat inside
R1(config-subif)##nat64 enable
R1(config)#int e1/2.20
R1(config-subif)##ip nat inside
R1(config-subif)##nat64 enable
R1(config)#int e1/2.30
R1(config-subif)##ip nat inside
R1(config-subif)##nat64 enable
R1(config)#int e1/2.40
R1(config-subif)##ip nat inside
R1(config-subif)##nat64 enable

#ip nat inside source list NAT interface e0/0 overload

#ip route 0.0.0.0 0.0.0.0 e0/0

